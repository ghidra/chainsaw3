package chainsaw.ui{  /*import flash.display.Sprite;  import flash.text.TextField;  import flash.events.*;  public class ui_text extends Sprite {	  private var button:Sprite = new Sprite();	  public function ui_text(s:String) {		  drawButton()		  button.addEventListener(MouseEvent.MOUSE_DOWN, mouseDownHandler);		  addChild(button);	  }	  private function drawButton():void {		  var textLabel:TextField = new TextField()		  button.graphics.clear();		  button.graphics.beginFill(0xD4D4D4); // grey color		  button.graphics.drawRoundRect(0, 0, 80, 25, 10, 10); // x, y, width, height, ellipseW, ellipseH		  button.graphics.endFill();		  textLabel.text = "Click Me!";		  textLabel.x = 10;		  textLabel.y = 5;		  textLabel.selectable = false;		  button.addChild(textLabel)	  }	  private function mouseDownHandler(event:MouseEvent):void {		  button.x += 20		  if (button.x > 400) { button.x = 0}	  }  }*/  import flash.display.Sprite;  import flash.text.TextField;  //import flash.events.*;  public class ui_text extends Sprite  {	  public var text_string:TextField=new TextField();	  	  public function ui_text(s:String="null",x:Number=0,y:Number=0,w:Number=200):void	  {  		  text_string.text = s;		  text_string.x = x;		  text_string.y = y;		  text_string.width=w;		  text_string.height=14;		  addChild(text_string);	  }  }}